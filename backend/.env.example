# ===================================
# DojoSoftware Backend Environment Variables
# ===================================
# HINWEIS: Dies ist eine Vorlage!
# Kopiere diese Datei zu .env und fülle die Werte aus.

# Datenbank-Konfiguration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=dein_sicheres_passwort_hier
DB_NAME=dojo
PORT=3002

# E-Mail Konfiguration (optional - für Vertrags-E-Mails)
# Für Gmail: smtp.gmail.com, Port 587
# Für andere: entsprechende SMTP-Einstellungen
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=deine_email@gmail.com
EMAIL_PASS=dein_email_passwort
EMAIL_FROM=noreply@dojosoftware.com

# JWT Secret (für Authentifizierung)
# Generiere ein sicheres Secret, z.B. mit: openssl rand -base64 32
JWT_SECRET=generiere_ein_sicheres_secret_hier

# Weitere Einstellungen
NODE_ENV=development
